---
ansible-navigator:
  ansible:
    config:
      path: ansible.cfg
  execution-environment:
    image: aap.rh.lab/ee-network-checkpoint:1.0
    pull:
      policy: missing
  playbook-artifact:
    enable: false
